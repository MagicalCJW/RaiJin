# application.yml

spring:
  profiles:
    active: dev
  application:
    name: raijin-service

server:
  error:
    whitelabel:
      enabled: false

itachi:
  common:
    sentry-dsn: ${SENTRY_DSN}
    deploy-env: ${DEPLOY:V2}
  signing-secret: ${SIGNING_SECRET}

