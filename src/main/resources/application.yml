# application.yml

spring:
  profiles:
    active: dev
  application:
    name: raijin-service

server:
  error:
    whitelabel:
      enabled: false

itachi:
  common:
    sentry-dsn: "https://public@sentry.example.com/1"
    deploy-env: "V2"
  signing-secret: "019"

